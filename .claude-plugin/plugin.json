{
  "name": "claude-plugin",
  "version": "0.5.0",
  "description": "Standalone Claude Code plugin by xgodev. Wires the `playwright` MCP server. No plugin dependencies -- other xgodev plugins are independent marketplaces (e.g. `quality-gate@xgodev-quality-gate`).",
  "author": {
    "name": "xgodev"
  },
  "repository": "https://github.com/xgodev/claude-plugin.git",
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"]
    }
  }
}

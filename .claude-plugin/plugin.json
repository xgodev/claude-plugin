{
  "name": "claude-plugin",
  "version": "1.4.4",
  "description": "All-in-one Claude Code plugin by xgodev. Bundles the `boost` skill set for github.com/xgodev/boost (Go service framework), the Quality Gate (dispatcher `qg` + per-language gates + `quality-gate` skill + opt-in pre-push hook), the `dev-rules` engineering-discipline skill with RED-first enforcement hooks, the `skill-rules` skill-authoring discipline, and the `ux-ui` design skill (searchable catalog + per-framework routing + UX methodology references).",
  "author": {
    "name": "xgodev"
  },
  "repository": "https://github.com/xgodev/claude-plugin.git",
  "license": "MIT",
  "dependencies": [
    {
      "name": "superpowers",
      "marketplace": "claude-plugins-official"
    }
  ]
}

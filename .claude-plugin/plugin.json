{
  "name": "claude-plugin",
  "version": "0.6.0",
  "description": "Umbrella Claude Code plugin by xgodev. Bundles no skills of its own; pulls `boost`, `quality-gate` and `dev-rules` via cross-marketplace dependencies and wires the `playwright` MCP server.",
  "author": {
    "name": "xgodev"
  },
  "repository": "https://github.com/xgodev/claude-plugin.git",
  "dependencies": [
    { "name": "boost", "marketplace": "xgodev-boost" },
    { "name": "quality-gate", "marketplace": "xgodev-quality-gate" },
    { "name": "dev-rules", "marketplace": "xgodev-dev-rules" }
  ],
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"]
    }
  }
}

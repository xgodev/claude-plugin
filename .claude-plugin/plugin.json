{
  "name": "claude-plugin",
  "version": "0.8.0",
  "description": "Umbrella Claude Code plugin by xgodev. Bundles no skills of its own; pulls `golang-boost`, `quality-gate`, `dev-rules` and `skill-rules` as dependencies from the same `xgodev-plugins` marketplace and wires the `playwright` MCP server.",
  "author": {
    "name": "xgodev"
  },
  "repository": "https://github.com/xgodev/claude-plugin.git",
  "dependencies": [
    "golang-boost",
    "quality-gate",
    "dev-rules",
    "skill-rules"
  ],
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"]
    }
  }
}

{
  "name": "claude-plugin",
  "version": "0.3.2",
  "description": "Umbrella Claude Code plugin. Bundles no skills of its own: provides the quality-gate skill via a dependency on the `quality-gate` plugin, pulls `boost`, and wires the `playwright` MCP server. Grows by adding dependencies.",
  "author": {
    "name": "xgodev"
  },
  "repository": "https://github.com/xgodev/claude-plugin.git",
  "dependencies": [
    "boost",
    "quality-gate"
  ],
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"]
    }
  }
}

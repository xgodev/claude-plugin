# Changelog

## [0.3.0]

- Skill `quality-gate` desbundlada; agora dependencia do plugin `quality-gate` (re-listado de github.com/xgodev/quality-gate no `marketplace.json` + declarado em `dependencies` no `plugin.json`). A skill e o gate vem empacotados nesse plugin dependido.
- `boost` (dependencia) e o servidor MCP `playwright` mantidos.

## [0.2.0]

- Dependência do plugin `boost` (re-listado de github.com/xgodev/boost no `marketplace.json` + declarado em `dependencies` no `plugin.json`).
- Servidor MCP `playwright` (`@playwright/mcp`, stdio via npx) embarcado em `mcpServers`.

## [0.1.0]

- Primeira versão pública.
